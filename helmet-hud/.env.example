# Optional: point at an external backend (e.g. Jetson) for video stream and HUD state.
# Leave unset to use in-browser mock video and mock state.
# VITE_WS_URL=http://localhost:8765
# VITE_STREAM_BASE=http://localhost:8765
